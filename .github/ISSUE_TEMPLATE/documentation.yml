name: 📚 文档改进
description: 报告文档中的问题或提出改进建议
title: "[Docs]: "
labels: ["documentation", "需要确认"]
body:
  - type: markdown
    attributes:
      value: |
        感谢您帮助改进 Coze2JianYing 的文档！清晰准确的文档对用户和开发者都很重要。

  - type: dropdown
    id: doc_type
    attributes:
      label: 文档类型
      description: 您要改进的是哪类文档？
      options:
        - README 主文档
        - 快速开始指南
        - API 文档
        - 工具函数文档
        - 代码注释
        - 示例和教程
        - 开发者指南
        - 其他
      default: 0
    validations:
      required: true

  - type: dropdown
    id: issue_type
    attributes:
      label: 问题类型
      description: 这是什么类型的文档问题？
      options:
        - 内容错误或过时
        - 信息缺失
        - 说明不清楚
        - 示例代码有误
        - 格式或排版问题
        - 链接失效
        - 翻译问题
        - 其他
      default: 0
    validations:
      required: true

  - type: textarea
    id: location
    attributes:
      label: 文档位置
      description: 请指明具体的文档文件和位置
      placeholder: |
        例如：
        - 文件：README.md，第 150 行附近
        - 章节：快速开始 > 创建插件步骤
        - 页面：https://github.com/Gardene-el/Coze2JianYing/blob/main/coze_plugin/tools/create_draft/README.md
    validations:
      required: true

  - type: textarea
    id: problem
    attributes:
      label: 当前问题
      description: 请清晰地描述当前文档存在的问题
      placeholder: |
        例如：
        - 步骤描述不够详细，新手难以理解
        - 示例代码缺少必要的导入语句
        - 参数说明与实际不符
        - 链接指向错误的页面
    validations:
      required: true

  - type: textarea
    id: suggestion
    attributes:
      label: 改进建议
      description: 请详细说明您建议如何改进
      placeholder: |
        例如：
        - 添加更详细的步骤说明和截图
        - 补充完整的代码示例
        - 更新参数说明为最新版本
        - 修正链接地址
        
        如果方便，可以直接提供改进后的文档内容。
    validations:
      required: true

  - type: textarea
    id: current_content
    attributes:
      label: 当前内容（可选）
      description: 如果是内容修改，可以粘贴当前的文档内容
      placeholder: |
        ```markdown
        当前的文档内容...
        ```
      render: markdown

  - type: textarea
    id: proposed_content
    attributes:
      label: 建议内容（可选）
      description: 如果您已经准备好改进后的内容，可以直接粘贴在这里
      placeholder: |
        ```markdown
        改进后的文档内容...
        ```
      render: markdown

  - type: dropdown
    id: priority
    attributes:
      label: 重要程度
      description: 您认为这个文档问题的重要程度如何？
      options:
        - 高 - 严重影响使用（错误信息、关键步骤缺失）
        - 中 - 影响用户体验（说明不清、信息不完整）
        - 低 - 小的改进（格式、措辞优化）
      default: 1
    validations:
      required: true

  - type: checkboxes
    id: contribution
    attributes:
      label: 贡献意愿
      description: 您是否愿意协助改进这部分文档？
      options:
        - label: 我可以提供改进后的文档内容
        - label: 我可以审阅修改后的文档
        - label: 我只是报告问题，由项目维护者处理

  - type: checkboxes
    id: terms
    attributes:
      label: 确认事项
      description: 在提交之前，请确认以下事项
      options:
        - label: 我已经搜索了现有的 Issues，确认这不是重复的文档问题
          required: true
        - label: 我已经检查了最新版本的文档
          required: true
        - label: 我提供了足够的信息来定位和改进文档
          required: true
