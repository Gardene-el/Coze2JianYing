# v1.0.0 Release 问题快速参考

## 🔍 问题现象

- **版本跳跃**：v0.0.4 → v1.0.0（跳过了 0.0.5, 0.1.0 等中间版本）
- **Release Notes 简陋**：只显示 "Initial Release"，没有详细的提交列表

## 💡 核心原因

### 1. 配置理解错误

```toml
major_on_zero = false  # 注释说"强制保持 0.x.x"，但实际效果相反！
```

**实际效果**：
- ✅ `major_on_zero = true` → 保持在 0.x.x，`feat` 触发 MINOR 升级
- ❌ `major_on_zero = false` → 遇到 `feat` 时升级到 1.0.0

### 2. 触发提交

提交 #179 "feat: 测试版本" 触发了版本升级逻辑。

### 3. Changelog 配置问题

```toml
exclude_commit_patterns = [
    "^Merge pull request",  # 排除了 PR 合并
    "^Merge branch",         # 排除了分支合并
]
```

导致大部分提交被排除在 Release Notes 之外。

## 🛠️ 快速解决方案

### 方案 A：接受 v1.0.0（推荐）

1. 保持当前配置
2. 更新文档说明版本升级原因
3. 继续使用标准 semantic versioning

### 方案 B：回退到 0.x.x

1. 删除 v1.0.0 tag 和 release
2. 修改配置：`major_on_zero = true`
3. 手动创建 v0.0.5
4. 发公告说明原因

## 📋 长期改进

1. **修正配置注释**
```toml
# 启用 Zero-Ver 模式：在 0.x.x 阶段，feat 触发 MINOR 而非 MAJOR
major_on_zero = true
```

2. **优化 Changelog 规则**
```toml
exclude_commit_patterns = [
    "^chore\\(release\\):",
    # 移除 merge 相关的排除
]
```

3. **规范提交格式**
- 使用 PR 模板
- 配置 Squash Merge
- 要求 PR 标题符合 Conventional Commits

## 📊 配置速查表

| 配置 | 效果 | 何时使用 |
|------|------|----------|
| `major_on_zero = true` | 0.x.x → 0.1.0 (feat) | 项目未稳定 |
| `major_on_zero = false` | 0.x.x → 1.0.0 (feat) | 准备发布 1.0 |

## 📚 详细报告

完整的调查分析请查看：[v1.0.0_release_analysis.md](./v1.0.0_release_analysis.md)
