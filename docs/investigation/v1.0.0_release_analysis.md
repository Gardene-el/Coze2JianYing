# v1.0.0 Release 版本号跳跃问题调查报告

## 问题描述

用户反映在 GitHub Releases 中发布了 v1.0.0 版本，但预期应该继续使用 0.x.x 版本号。同时，Release Notes 中缺少详细的提交信息和修改细节。

### 用户预期
1. **版本号管理**：在未使用 `feat!` 等破坏性变更标记的情况下，不应该更新大版本号（从 v0.0.4 跳到 v1.0.0）
2. **Release Notes**：应该包含从 develop 合并的所有 commit 的详细信息

### 实际情况
1. **版本号**：从 v0.0.4 直接跳到了 v1.0.0
2. **Release Notes**：只显示 "Initial Release"，没有详细的提交列表

## 调查过程

### 1. 配置文件检查

#### pyproject.toml 配置

```toml
[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
version_variables = ["app/__init__.py:__version__"]
branch = "main"
upload_to_vcs_release = true
upload_to_pypi = false
build_command = "python build.py"
commit_message = "chore(release): {version} [skip ci]"
major_on_zero = false  # 强制保持 0.x.x 版本，feat 只触发 MINOR 升级

[tool.semantic_release.changelog]
exclude_commit_patterns = [
    "^chore\\(release\\):",
    "^Merge pull request",
    "^Merge branch",
]

[tool.semantic_release.commit_parser_options]
allowed_tags = [
    "build",
    "chore",
    "ci",
    "docs",
    "feat",
    "fix",
    "perf",
    "style",
    "refactor",
    "test",
]
minor_tags = ["feat"]
patch_tags = ["fix", "perf"]
```

**关键发现**：
- `major_on_zero = false` 已配置，但其实际含义与预期**相反**
- 配置排除了 merge commit，这会导致 Release Notes 缺少合并的提交详情

### 2. GitHub Actions 工作流日志分析

#### 触发 v1.0.0 发布的关键信息

**Workflow Run ID**: 19375918978  
**触发时间**: 2025-11-14T19:51:07Z  
**触发分支**: main  
**触发 Commit**: 1667bf5 ("build: 尝试自动发布Release (#187)")

#### Semantic Release 执行日志

```
=== 运行 semantic-release version --print ===
Exit code: 0
Output:
[19:52:17] WARNING  Token value is missing!                       config.py:782 
1.0.0
##[notice]检测到需要发布的更新

The next version is: 1.0.0! 🚀
```

**关键发现**：semantic-release **直接决定**下一个版本是 1.0.0，没有经过 0.x.x 阶段。

### 3. 提交历史分析

#### v0.0.4 到 v1.0.0 之间的提交

从 v0.0.4 到触发 v1.0.0 的 commit (1667bf5) 之间有 **15 个提交**：

```
1. Initial plan
2. chore(release): 1.0.0 [skip ci]
3. build: 尝试自动发布Release (#187)
4. Develop (#186)
5. Develop (#185)
6. Merge branch 'develop' of ...
7. Develop (#184)
8. Develop (#183)
9. fix: 测试 (#182)
10. fix: see if merge (#181)
11. 更新当前项目管理框架 (#180)
12. feat: 测试版本 (#179)  ← 关键提交
13. 测试推送 (#178)
14. 更新当前项目配置 (#177)
15. Merge pull request #176 from Gardene-el/develop
```

**关键发现**：
- Commit #12 "feat: 测试版本 (#179)" 是一个 `feat` 类型的提交
- 多个 merge commit 和普通提交都被排除在 changelog 之外

### 4. 根本原因分析

#### 为什么跳到 v1.0.0？

经过深入分析 Python Semantic Release 的行为和文档，发现了核心问题：

**`major_on_zero` 配置的实际含义**：

这个配置的含义与注释中的说明**完全相反**：

- **`major_on_zero = true`**（默认值）：启用 "Zero-Ver" 模式
  - 在 0.x.x 版本时，`feat` → 触发 MINOR 升级 (0.0.x → 0.1.0)
  - 在 0.x.x 版本时，`BREAKING CHANGE` → 仍然只触发 MINOR 升级（保持在 0.x.x）
  - **适用场景**：项目仍在快速迭代，尚未稳定
  
- **`major_on_zero = false`**（当前配置）：禁用 "Zero-Ver" 模式
  - 在 0.x.x 版本时，`feat` → 视为准备离开 0.x.x，**触发 MAJOR 升级到 1.0.0**
  - **适用场景**：项目已经成熟，准备发布 1.0.0 正式版本

**因此**：当前配置 `major_on_zero = false` + commit #179 的 `feat:` 提交 = 触发升级到 v1.0.0

#### 配置注释错误

pyproject.toml 中的注释：
```toml
major_on_zero = false  # 强制保持 0.x.x 版本，feat 只触发 MINOR 升级
```

这个注释是**错误的**！实际行为恰恰相反。

#### v0.0.4 为什么没有升级？

查看历史，v0.0.4 之前可能：
1. 没有 `feat` 类型的提交
2. 或者版本号是手动设置的

但这次 commit #179 "feat: 测试版本" 触发了 semantic-release 的判断，执行了从 0.x.x 到 1.0.0 的升级。

#### 为什么 Release Notes 缺少详细信息？

1. **Changelog 排除规则过于严格**：
```toml
exclude_commit_patterns = [
    "^chore\\(release\\):",
    "^Merge pull request",  # 排除了所有 PR 合并
    "^Merge branch",         # 排除了所有分支合并
]
```

这意味着形如 "Develop (#186)"、"Merge pull request #176" 的提交都被排除了。

2. **Commit 消息格式不规范**：
   - "Develop (#186)" - 没有类型前缀（应该是 `feat:` 或 `fix:` 等）
   - "更新当前项目管理框架 (#180)" - 中文且无类型前缀
   - "测试推送 (#178)" - 无类型前缀

   只有符合 Conventional Commits 格式的提交才会被包含在 Release Notes 中。

3. **"Initial Release" 标记**：
   - 这是 main 分支第一次正式运行 semantic-release
   - 之前的 v0.0.4 可能是手动创建的 release
   - semantic-release 没有找到足够的符合格式的提交来生成详细的 changelog

## 解决方案建议

### 1. 保持在 0.x.x 版本的正确配置

如果要在 0.x.x 阶段持续开发，应该：

```toml
[tool.semantic_release]
# ... 其他配置保持不变 ...
major_on_zero = true  # 改为 true，启用 Zero-Ver 模式
```

**配置说明**：
- `major_on_zero = true`：保持在 0.x.x，`feat` 只触发 MINOR（0.0.x → 0.1.0）
- `major_on_zero = false`：准备离开 0.x.x，`feat` 触发升级到 1.0.0

同时**更正注释**：

```toml
# 启用 Zero-Ver 模式：在 0.x.x 阶段，feat 触发 MINOR 升级而非 MAJOR
major_on_zero = true
```

### 2. 优化 Changelog 生成

#### 方案 A：减少排除规则（推荐）

```toml
[tool.semantic_release.changelog]
exclude_commit_patterns = [
    "^chore\\(release\\):",
    # 只保留 release commit 的排除，移除 merge commit 的排除
]
```

或者更精确的排除：

```toml
[tool.semantic_release.changelog]
exclude_commit_patterns = [
    "^chore\\(release\\):",
    "^Merge branch '(develop|main)'",  # 只排除分支合并，保留 PR 合并
]
```

#### 方案 B：改进提交消息规范

强制要求所有 PR 使用规范的标题格式：
- `feat: 添加新功能`
- `fix: 修复bug`  
- `docs: 更新文档`
- `chore: 杂项任务`

可以配置 GitHub 分支保护规则，要求 PR 标题符合格式。

#### 方案 C：使用 Squash Merge

在 GitHub 仓库设置中：
1. Settings → General → Pull Requests
2. 启用 "Allow squash merging"
3. 禁用其他合并方式

这样每个 PR 合并时会变成一个符合格式的 commit，便于 semantic-release 解析。

### 3. 当前版本号的处理

#### 选项 1：接受 v1.0.0 并继续（推荐）

**理由**：
- v1.0.0 已经发布，回退可能造成混乱
- 可以视为项目已经进入稳定阶段
- 继续使用 semantic versioning 正常流程

**后续操作**：
1. 保持 `major_on_zero = false` 配置不变
2. 更新项目文档说明版本升级原因
3. 继续使用标准的 `feat`/`fix` 等提交格式
4. 下次 `feat` → v1.1.0, `fix` → v1.0.1

#### 选项 2：回退到 0.x.x（不推荐，但可行）

**步骤**：
1. 删除 v1.0.0 的 tag 和 release
```bash
git tag -d v1.0.0
git push origin :refs/tags/v1.0.0
# 在 GitHub UI 中删除 Release
```

2. 修改配置并手动创建 v0.0.5
```toml
[tool.semantic_release]
major_on_zero = true  # 改为 true
```

```bash
# 手动更新版本号
# 在 pyproject.toml 和 app/__init__.py 中设置为 0.0.5
git tag v0.0.5
git push origin v0.0.5
# 在 GitHub UI 中创建 Release
```

3. 重新提交配置
```bash
git add pyproject.toml
git commit -m "fix: 修正 semantic-release 配置，保持在 0.x.x 版本"
git push
```

**风险**：
- 已经有用户下载了 v1.0.0 的文件
- 需要发公告说明版本回退原因
- 可能造成版本混乱

### 4. 长期改进建议

#### 4.1 建立清晰的分支策略

```
develop (开发分支)
    ↓
    Pull Request (使用 Squash Merge)
    ↓
main (发布分支)
    ↓
    semantic-release 自动发布
```

**规范**：
- 所有开发在 `develop` 分支进行
- 通过 PR 合并到 `main`
- PR 标题必须符合 Conventional Commits 格式
- 使用 Squash Merge，确保每个 PR 对应一个规范的 commit

#### 4.2 使用 PR 模板

创建 `.github/pull_request_template.md`：

```markdown
## 变更类型
<!-- 选择一个类型作为 PR 标题的前缀 -->
- [ ] feat: 新功能
- [ ] fix: Bug修复
- [ ] docs: 文档更新
- [ ] style: 代码风格调整（不影响功能）
- [ ] refactor: 代码重构
- [ ] perf: 性能优化
- [ ] test: 测试相关
- [ ] chore: 构建/工具/依赖更新

## 变更描述
<!-- 详细描述你的更改，这将作为 changelog 的内容 -->

## 破坏性变更
<!-- 如果有破坏性变更，请在 PR 标题末尾添加 ! -->
- [ ] 是（在标题末尾添加 `!`，如 `feat!: xxx`）
- [ ] 否

## 测试
<!-- 说明如何测试这些更改 -->

## 相关 Issue
<!-- 关联相关的 Issue，使用 #issue_number -->
```

#### 4.3 配置 Commit Message Lint（可选）

如果想在提交阶段就进行检查，可以配置 commitlint：

```bash
npm install --save-dev @commitlint/cli @commitlint/config-conventional
npm install --save-dev husky
```

创建 `.commitlintrc.json`：
```json
{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      2,
      "always",
      ["build", "chore", "ci", "docs", "feat", "fix", "perf", "refactor", "style", "test"]
    ]
  }
}
```

#### 4.4 GitHub Branch Protection Rules

在仓库设置中配置分支保护：
1. Settings → Branches → Add rule
2. Branch name pattern: `main`
3. 勾选：
   - Require pull request reviews before merging
   - Require status checks to pass before merging
   - Require linear history (强制使用 squash merge)

## 总结

### 问题根因

1. **版本号跳跃**：
   - `major_on_zero = false` 配置的实际含义与注释**相反**
   - 应该使用 `major_on_zero = true` 来保持在 0.x.x 阶段
   - `feat: 测试版本` 提交触发了从 0.x.x 到 1.0.0 的升级

2. **Release Notes 缺失**：
   - Changelog 排除了所有 merge commits
   - 大多数提交消息格式不规范，无法被识别为有效的 changelog 条目
   - 首次运行 semantic-release 导致显示 "Initial Release"

### 关键配置对比

| 配置值 | 行为 | 适用场景 |
|--------|------|----------|
| `major_on_zero = true` | 0.x.x 阶段，`feat` → MINOR (0.0.x → 0.1.0) | 项目仍在快速迭代，未稳定 |
| `major_on_zero = false` | 0.x.x 阶段，`feat` → MAJOR (0.x.x → 1.0.0) | 项目已成熟，准备发布 1.0 |

### 建议行动

#### 短期（立即执行）：
1. ✅ **决定版本策略**：接受 v1.0.0 或回退到 0.x.x
2. 📝 在项目 README 中说明版本升级原因（如果接受 v1.0.0）
3. 🔧 修正 pyproject.toml 中的配置注释

#### 中期（1-2周内）：
1. 🔧 根据决定调整 `major_on_zero` 配置
2. 📋 创建 PR 模板，规范提交格式
3. 📚 优化 changelog 排除规则
4. 🔍 配置 GitHub branch protection rules

#### 长期（持续改进）：
1. 🚀 规范化 Git 工作流（develop → PR → main）
2. 📖 建立版本管理和提交规范文档
3. 🤖 考虑添加 commitlint 等自动化检查

## 参考资料

- [Python Semantic Release 文档](https://python-semantic-release.readthedocs.io/)
- [major_on_zero 配置说明](https://python-semantic-release.readthedocs.io/en/latest/configuration.html#config-major-on-zero)
- [Conventional Commits 规范](https://www.conventionalcommits.org/)
- [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)
- [GitHub Pull Request 最佳实践](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests)
