/**
 * Placeholder — coze2jianying does not yet have an auto-updater IPC bridge.
 * Returns null so the TitleBar layout is unaffected.
 *
 * When auto-update support is added, implement this component to watch
 * for 'updateDownloaded' IPC broadcasts and prompt the user.
 */
export const UpdateNotification: React.FC = () => null;
