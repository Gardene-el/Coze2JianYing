# 子进程方式运行 FastAPI 服务 - 完整指南

## 🎯 新方案概述

我们已经将 FastAPI 服务从**线程模式**升级为**子进程模式**，实现了完整的日志捕获和实时监控。

### ✅ 新方案优势

| 特性          | 线程模式                | 子进程模式 ⭐             |
| ------------- | ----------------------- | ------------------------- |
| 日志捕获      | ❌ 受限（GUI 环境问题） | ✅ 完整捕获               |
| 实时监控      | ❌ 难以实现             | ✅ 实时显示               |
| 进程隔离      | ⚠️ 共享内存空间         | ✅ 完全隔离               |
| 停止服务      | ⚠️ 困难（线程难终止）   | ✅ 简单（terminate/kill） |
| 稳定性        | ⚠️ 可能影响主程序       | ✅ 互不影响               |
| stdout/stderr | ❌ 受 GUI 限制          | ✅ 完全可用               |

---

## 🔧 技术实现

### 1. 架构图

```
GUI 主进程 (Tkinter)
    │
    ├─► 启动按钮点击
    │       ↓
    │   创建子进程 (subprocess.Popen)
    │       ↓
    │   uvicorn 在子进程中运行
    │       ↓
    │   FastAPI 应用运行
    │
    ├─► 日志读取线程
    │       ↓
    │   持续读取子进程的 stdout
    │       ↓
    │   将日志放入队列 (Queue)
    │
    └─► GUI 主线程
            ↓
        定期从队列读取日志
            ↓
        更新日志显示窗口
```

### 2. 核心组件

#### (1) 子进程管理

```python
# 启动子进程
self.service_process = subprocess.Popen(
    [python_exe, "-m", "uvicorn", "app.api_main:app", ...],
    stdout=subprocess.PIPE,      # 捕获标准输出
    stderr=subprocess.STDOUT,     # 将错误重定向到标准输出
    text=True,                    # 文本模式（而非字节）
    bufsize=1,                    # 行缓冲
    encoding='utf-8',             # UTF-8 编码
    errors='replace'              # 替换无法解码的字符
)
```

**关键参数解释：**

- `stdout=subprocess.PIPE`：将子进程的输出重定向到管道，我们可以读取
- `stderr=subprocess.STDOUT`：将错误输出合并到标准输出（统一处理）
- `text=True`：以文本模式读取（而非字节流）
- `bufsize=1`：行缓冲，每次读取一行
- `encoding='utf-8'`：使用 UTF-8 编码解析输出
- `errors='replace'`：遇到无法解码的字符用 `?` 替换

#### (2) 日志读取线程

```python
def _read_process_output(self):
    """在后台线程中持续读取子进程输出"""
    try:
        # 逐行读取输出
        for line in iter(self.service_process.stdout.readline, ''):
            if not line:
                break
            line = line.rstrip()  # 去除末尾换行符
            if line:
                self.log_queue.put(line)  # 放入队列

            if self.stop_event.is_set():
                break
    except Exception as e:
        self.logger.error(f"读取进程输出时出错: {e}")
```

**工作原理：**

1. `iter(readline, '')` 创建迭代器，持续读取直到空字符串
2. 每读取一行，放入队列 `self.log_queue`
3. 在后台线程运行，不阻塞 GUI
4. 检查 `stop_event` 以便正常停止

#### (3) GUI 日志更新

```python
def _update_log_display(self):
    """在 GUI 主线程中定期更新日志显示"""
    try:
        # 从队列中取出所有可用日志
        while not self.log_queue.empty():
            log_line = self.log_queue.get_nowait()
            self._append_to_info(log_line)

        # 如果服务运行中，100ms 后再次调用
        if self.service_running:
            self.frame.after(100, self._update_log_display)
    except Exception as e:
        self.logger.error(f"更新日志显示时出错: {e}")
```

**工作原理：**

1. 从队列中获取所有可用的日志行
2. 追加到 GUI 的文本框中
3. 使用 `frame.after(100, ...)` 每 100ms 调用一次
4. 只在主线程操作 GUI 组件（线程安全）

### 3. 进程间通信

```
子进程 (FastAPI)          日志队列          GUI 主线程
    │                      (Queue)               │
    ├─ 输出日志 ──────►  [log1, log2, log3]      │
    │                         │                   │
    │                         └───────────────►   │
    │                                         显示日志
    ├─ 更多日志 ──────►  [log4, log5]            │
    │                         │                   │
    │                         └───────────────►   │
```

**为什么使用队列（Queue）？**

- ✅ **线程安全**：多线程环境下可安全读写
- ✅ **异步通信**：读取线程和 GUI 线程解耦
- ✅ **缓冲机制**：避免日志丢失
- ✅ **非阻塞**：`get_nowait()` 不会阻塞 GUI

---

## 🚀 使用指南

### 启动服务

1. **打开 GUI 应用**：

   ```powershell
   python app/main.py
   ```

2. **切换到"本地服务"标签页**

3. **配置端口**（可选，默认 8000）

4. **点击"启动服务"**

5. **观察实时日志**：
   ```
   [10:30:45] 正在启动服务...
   [10:30:45] 使用子进程模式，可完整捕获日志
   [10:30:45] 服务进程已启动 (PID: 12345)
   [10:30:45] 访问地址: http://localhost:8000
   [10:30:45] API文档: http://localhost:8000/docs
   [10:30:45] ------------------------------------------------------------
   INFO:     Started server process [12345]
   INFO:     Waiting for application startup.
   INFO:     Application startup complete.
   INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
   ```

### 停止服务

1. **点击"停止服务"按钮**

2. **观察停止过程**：
   ```
   [10:35:20] 正在停止服务...
   INFO:     Shutting down
   INFO:     Finished server process [12345]
   [10:35:22] 服务进程已正常终止
   [10:35:22] 服务已停止
   ```

### 清空日志

- **点击"清空日志"按钮** - 清除显示的日志，不影响运行

---

## 📊 日志内容解析

### 典型启动日志

```
[10:30:45] 服务进程已启动 (PID: 12345)
# ↑ GUI 添加的时间戳和进程ID

INFO:     Started server process [12345]
# ↑ uvicorn 输出：服务器进程启动

INFO:     Waiting for application startup.
# ↑ 等待 FastAPI 应用启动

INFO:     Application startup complete.
# ↑ 应用启动完成

INFO:     Uvicorn running on http://127.0.0.1:8000
# ↑ 服务器运行地址
```

### API 请求日志

当有请求时，你会看到：

```
INFO:     127.0.0.1:54321 - "GET /api/example/health HTTP/1.1" 200 OK
# ↑ 客户端IP:端口 - "请求方法 路径 协议" 状态码
```

### 错误日志

如果有错误：

```
ERROR:    Exception in ASGI application
Traceback (most recent call last):
  ...
# ↑ 完整的错误堆栈跟踪
```

---

## 🎨 UI 增强

### 新增功能

1. **深色主题日志窗口**：

   - 背景色：`#1e1e1e`（深灰）
   - 文字色：`#d4d4d4`（浅灰）
   - 类似专业 IDE 的终端

2. **滚动条**：

   - 自动滚动到最新日志
   - 可手动滚动查看历史

3. **清空日志按钮**：

   - 快速清除显示内容
   - 不影响服务运行

4. **更大的日志窗口**：
   - 从 8 行增加到 12 行
   - 更好的查看体验

---

## 🔍 进程管理详解

### 启动进程

```python
# 构建命令
cmd = [
    sys.executable,          # 当前 Python 解释器
    "-m", "uvicorn",         # 运行 uvicorn 模块
    "app.api_main:app",      # FastAPI 应用位置
    "--host", "127.0.0.1",   # 监听地址
    "--port", "8000",        # 监听端口
    "--log-level", "info",   # 日志级别
]

# 启动
process = subprocess.Popen(cmd, ...)
```

### 停止进程

```python
# 方式1: 优雅终止（推荐）
process.terminate()  # 发送 SIGTERM 信号
process.wait(timeout=5)  # 等待进程结束

# 方式2: 强制终止（超时后）
if process.poll() is None:  # 如果还在运行
    process.kill()  # 发送 SIGKILL 信号（强制）
```

**terminate() vs kill()：**

| 方法          | 信号    | 行为                 | 使用场景         |
| ------------- | ------- | -------------------- | ---------------- |
| `terminate()` | SIGTERM | 优雅退出，可清理资源 | 首选方法         |
| `kill()`      | SIGKILL | 立即强制终止         | terminate 无效时 |

### 进程状态检查

```python
# 检查进程是否在运行
if process.poll() is None:
    print("进程正在运行")
else:
    print(f"进程已退出，返回码: {process.returncode}")

# 获取进程ID
pid = process.pid
```

---

## 🐛 故障排查

### 问题 1: 日志显示乱码

**原因**：编码问题

**解决**：

```python
# 已在代码中设置
subprocess.Popen(
    ...,
    encoding='utf-8',
    errors='replace'  # 替换无法解码的字符
)
```

### 问题 2: 日志不实时更新

**原因**：缓冲问题

**解决**：

```python
# 已设置行缓冲
subprocess.Popen(
    ...,
    bufsize=1  # 行缓冲，每行立即刷新
)
```

### 问题 3: 服务无法停止

**原因**：进程未正确终止

**解决**：

1. 先尝试 `terminate()`（等待 5 秒）
2. 如果失败，使用 `kill()` 强制终止
3. 代码已实现此逻辑

### 问题 4: 关闭 GUI 后进程仍在运行

**原因**：清理逻辑未执行

**解决**：

- 已在 `cleanup()` 方法中实现
- GUI 关闭时自动终止子进程

---

## 📈 性能考虑

### CPU 使用

- **日志读取线程**：极低（阻塞式读取）
- **GUI 更新**：低（100ms 间隔）
- **子进程**：取决于 API 请求量

### 内存使用

- **日志队列**：动态增长，及时消费
- **子进程**：独立内存空间
- **GUI**：文本框自动管理

### 优化建议

1. **限制日志行数**：

   ```python
   # 可添加到 _append_to_info 中
   line_count = int(self.info_text.index('end-1c').split('.')[0])
   if line_count > 1000:  # 超过1000行
       self.info_text.delete(1.0, "500.0")  # 删除前500行
   ```

2. **调整更新频率**：
   ```python
   # 如果日志量大，可增加间隔
   self.frame.after(200, self._update_log_display)  # 200ms
   ```

---

## 🎓 学习要点

### 1. subprocess 模块

**为什么用子进程而不是线程？**

```python
# 线程的问题
import threading
thread = threading.Thread(target=func)
thread.start()
# ❌ 共享内存空间，可能冲突
# ❌ 难以真正终止
# ❌ GIL（全局解释器锁）限制性能

# 子进程的优势
import subprocess
process = subprocess.Popen(...)
# ✅ 独立内存空间
# ✅ 可以 terminate/kill
# ✅ 真正的并行执行
# ✅ 可捕获 stdout/stderr
```

### 2. Queue（队列）

**为什么需要队列？**

```python
from queue import Queue

# 没有队列的问题
logs = []  # 普通列表
# ❌ 不是线程安全的
# ❌ 需要加锁（Lock）

# 使用队列
log_queue = Queue()
# ✅ 内部已实现线程安全
# ✅ 阻塞/非阻塞操作
log_queue.put(item)      # 放入
item = log_queue.get()   # 取出（阻塞）
item = log_queue.get_nowait()  # 取出（非阻塞）
```

### 3. Tkinter 的 after 方法

**为什么用 after 而不是 while 循环？**

```python
# 错误方式
while True:
    update_gui()
    time.sleep(0.1)
# ❌ 阻塞主循环
# ❌ GUI 无响应

# 正确方式
def update_gui():
    # 更新内容
    root.after(100, update_gui)  # 100ms后再次调用
# ✅ 不阻塞事件循环
# ✅ GUI 保持响应
```

---

## 📚 扩展阅读

### Python 文档

- [subprocess 模块](https://docs.python.org/3/library/subprocess.html)
- [queue 模块](https://docs.python.org/3/library/queue.html)
- [threading 模块](https://docs.python.org/3/library/threading.html)

### 相关教程

- [进程间通信](https://realpython.com/python-subprocess/)
- [Tkinter 异步操作](https://realpython.com/python-gui-tkinter/)

---

## 🎉 总结

现在你的 GUI 应用具备了：

✅ **完整的日志捕获** - 所有 uvicorn/FastAPI 日志都可见  
✅ **实时监控** - 100ms 延迟的实时日志更新  
✅ **进程隔离** - 服务崩溃不影响 GUI  
✅ **可靠停止** - terminate + kill 双重保障  
✅ **专业界面** - 深色主题，滚动条，清空功能  
✅ **高性能** - 异步读取，队列缓冲

这是一个**生产级的实现**，可以用于实际项目中！🚀
