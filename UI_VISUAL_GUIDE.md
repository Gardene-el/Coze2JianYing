# Visual UI Changes - Coze API Settings

## 📸 Screenshot Placeholder

Since this is a headless environment, here's what the UI looks like:

```
╔══════════════════════════════════════════════════════════════════════════╗
║                  Coze剪映草稿生成器 - 本地服务标签页                      ║
╠══════════════════════════════════════════════════════════════════════════╣
║                                                                          ║
║  ┌──────────────────────────────────────────────────────────────────┐  ║
║  │ 草稿文件夹设置                                                     │  ║
║  ├──────────────────────────────────────────────────────────────────┤  ║
║  │  剪映草稿文件夹: │ 未选择（将使用默认路径）  │ │选择...│ │自动检测│ │  ║
║  └──────────────────────────────────────────────────────────────────┘  ║
║                                                                          ║
║  ┌──────────────────────────────────────────────────────────────────┐  ║
║  │ 🆕 Coze API 配置                                                  │  ║
║  ├──────────────────────────────────────────────────────────────────┤  ║
║  │                                                                   │  ║
║  │  API Token:  │ ******************************** │  ☐ 显示       │  ║
║  │                                                                   │  ║
║  │  服务地址:   │ https://api.coze.cn            ▼│                 │  ║
║  │                                                                   │  ║
║  │  状态: 未配置                               │  测试连接  │        │  ║
║  │                                                                   │  ║
║  └──────────────────────────────────────────────────────────────────┘  ║
║                                                                          ║
║  ┌──────────────────────────────────────────────────────────────────┐  ║
║  │ FastAPI 服务管理                                                   │  ║
║  ├──────────────────────────────────────────────────────────────────┤  ║
║  │                                                                   │  ║
║  │  端口: │ 8000 │                                 │  检测端口  │    │  ║
║  │                                                                   │  ║
║  │  ◯ 端口状态: 未检测                                               │  ║
║  │                                                                   │  ║
║  │  ● 服务状态: 未启动                                               │  ║
║  │                                                                   │  ║
║  │  │  启动服务  │  │  停止服务  │                                  │  ║
║  │                                                                   │  ║
║  │  ┌──────────────────────────────────────────────────────────┐   │  ║
║  │  │ 服务实时日志                                              │   │  ║
║  │  │                                                           │   │  ║
║  │  │ [日志输出区域 - 深色背景]                                 │   │  ║
║  │  │                                                           │   │  ║
║  │  │                                              │  清空日志  │   │  ║
║  │  └──────────────────────────────────────────────────────────┘   │  ║
║  │                                                                   │  ║
║  └──────────────────────────────────────────────────────────────────┘  ║
║                                                                          ║
║  状态栏: 就绪                                                            ║
╚══════════════════════════════════════════════════════════════════════════╝
```

## 🎯 Key Features Highlighted

### 1️⃣ API Token Field (Password Protected)
```
┌─────────────────────────────────────────────┐
│ API Token:  ******************************** │  ☐ 显示
└─────────────────────────────────────────────┘
         ↑                                  ↑
    Hidden by default              Toggle to show
```

**Features**:
- Default: Shows as `*` (asterisks)
- Width: 50 characters
- No persistence (memory only)
- Checkbox to toggle visibility

### 2️⃣ Base URL Selector
```
┌─────────────────────────────────────┐
│ 服务地址:  https://api.coze.cn    ▼│
└─────────────────────────────────────┘
              ↑
         Default: CN version
         
Options:
  • https://api.coze.cn  (国内版)
  • https://api.coze.com (国际版)
```

**Features**:
- Dropdown (Combobox)
- Read-only
- Two options (CN/COM)
- Default: Chinese version

### 3️⃣ Status Display with Test Button
```
┌────────────────────────────────────────────┐
│ 状态: 未配置              │  测试连接  │   │
└────────────────────────────────────────────┘
        ↑                        ↑
   Dynamic status         Validation button

Status States:
  ⚪ 未配置 (gray)
  🔄 测试连接中... (black)
  ✅ 已配置 ✓ (green)
  ❌ 连接失败 ✗ (red)
```

**Features**:
- Real-time status updates
- Color-coded feedback
- Test button validates credentials
- Error messages with details

## 🔄 State Transitions

### Initial State → Testing
```
┌──────────────────────────────────────────┐
│ 状态: 未配置        │  测试连接  │ ──┐   │
└──────────────────────────────────────────┘  │
                                              │ Click
                                              ↓
┌──────────────────────────────────────────┐
│ 状态: 测试连接中... │  测试连接  │      │
└──────────────────────────────────────────┘
```

### Testing → Success
```
┌──────────────────────────────────────────┐
│ 状态: 测试连接中... │  测试连接  │      │
└──────────────────────────────────────────┘
              ↓ API validation success
┌──────────────────────────────────────────┐
│ 状态: 已配置 ✓      │  测试连接  │      │
└──────────────────────────────────────────┘
         (green)
```

### Testing → Failure
```
┌──────────────────────────────────────────┐
│ 状态: 测试连接中... │  测试连接  │      │
└──────────────────────────────────────────┘
              ↓ API validation failed
┌──────────────────────────────────────────┐
│ 状态: 连接失败 ✗    │  测试连接  │      │
└──────────────────────────────────────────┘
         (red)
```

## 🔐 Security Features Visual

### Password Protection
```
Default View (Hidden):
┌────────────────────────────────┐
│ ******************************** │  ☐ 显示
└────────────────────────────────┘

Revealed View (When checked):
┌────────────────────────────────┐
│ pat_ABcd1234EFgh5678IJkl9012... │  ☑ 显示
└────────────────────────────────┘
```

### No Persistence Indication
```
💾 Storage Location: Memory Only
├── ❌ Not saved to file
├── ❌ Not saved to environment variables
├── ❌ Not saved to registry
└── ✅ Cleared on application exit
```

## 📱 Responsive Layout

### Component Spacing
```
┌─────────────────────────────────────────────────────────────┐
│ [Label]     [Input Field................] [Button/Checkbox] │
│   ↑              ↑                              ↑           │
│  Fixed      Expandable (weight=1)           Fixed          │
└─────────────────────────────────────────────────────────────┘
```

### Grid Layout
```
Row 0: Folder Settings Frame
       ├── Takes full width
       └── Fixed height

Row 1: Coze API Config Frame  🆕 NEW
       ├── Takes full width
       └── Auto height (3 internal rows)

Row 2: FastAPI Service Frame
       ├── Takes full width
       └── Expandable (weight=1)

Row 3: Status Bar
       └── Takes full width (fixed height)
```

## 🎨 Color Scheme

### Status Colors
```
┌──────────────────────────────────────┐
│ Status          Color      Example   │
├──────────────────────────────────────┤
│ 未配置          Gray       ⚪        │
│ 测试连接中...    Black      ⚫        │
│ 已配置 ✓        Green      🟢        │
│ 连接失败 ✗      Red        🔴        │
└──────────────────────────────────────┘
```

### Input Fields
```
┌─────────────────────────────────────┐
│ Component       Background  Text    │
├─────────────────────────────────────┤
│ Token Entry     White       Black   │
│ URL Combo       White       Black   │
│ Status Label    System      Dynamic │
└─────────────────────────────────────┘
```

## 🔧 Interactive Elements

### Token Visibility Toggle
```
┌────────────────────────────────────────────┐
│                                            │
│  [***********]  ☐ 显示  ←─── Unchecked    │
│       ↓                                    │
│       └─ Shows as asterisks                │
│                                            │
│  [pat_abc123]   ☑ 显示  ←─── Checked      │
│       ↓                                    │
│       └─ Shows plain text                  │
│                                            │
└────────────────────────────────────────────┘
```

### URL Dropdown
```
┌──────────────────────────────────┐
│ https://api.coze.cn            ▼│ ←─ Click
└──────────────────────────────────┘
              ↓
┌──────────────────────────────────┐
│ https://api.coze.cn            ▲│
├──────────────────────────────────┤
│ • https://api.coze.cn            │ ←─ Selected
│   https://api.coze.com           │
└──────────────────────────────────┘
```

### Test Connection Flow
```
┌────────────┐
│ 测试连接   │ ←─ Click
└────────────┘
      ↓
   Validate
      ↓
  ┌───────┐
  │ Check │
  │ Token │
  └───┬───┘
      │
  ┌───▼──────┐
  │ Create   │
  │ Client   │
  └───┬──────┘
      │
  ┌───▼──────┐
  │ Update   │
  │ Status   │
  └───┬──────┘
      │
  ┌───▼──────┐
  │ Show     │
  │ Result   │
  └──────────┘
```

## 💡 Usage Tips

### Recommended Workflow
```
1️⃣  Enter Token
    └─→ Type or paste your API token

2️⃣  Select URL
    └─→ Choose CN or COM version

3️⃣  Test Connection
    └─→ Click to validate

4️⃣  Check Status
    └─→ Look for green ✓

5️⃣  Use Service
    └─→ Start FastAPI service
```

### Error Recovery
```
❌ If connection fails:
   ├─→ Check token validity
   ├─→ Verify network connection
   ├─→ Confirm correct URL
   └─→ Click "测试连接" again
```

## 📏 Dimensions

### Component Sizes
```
┌────────────────────────────────────────┐
│ Component        Width      Height     │
├────────────────────────────────────────┤
│ Token Entry      50 chars   1 line     │
│ Show Checkbox    Auto        1 line     │
│ URL Combo        30 chars   1 line     │
│ Status Label     Auto        1 line     │
│ Test Button      Auto        1 line     │
└────────────────────────────────────────┘
```

### Frame Padding
```
┌─────────────────────────────┐
│  Coze API 配置   ← Title    │
├─────────────────────────────┤
│ ↕ 5px padding               │
│ ← 5px → Content ← 5px →     │
│ ↕ 5px padding               │
└─────────────────────────────┘
```

## 🎬 Animation States

### Status Transition
```
⚪ 未配置
   ↓ (instant)
⚫ 测试连接中...
   ↓ (async wait)
🟢 已配置 ✓  OR  🔴 连接失败 ✗
```

### Button States
```
Normal:     │  测试连接  │
Hover:      │  测试连接  │  (system highlight)
Click:      │  测试连接  │  (system pressed)
Disabled:   │  测试连接  │  (grayed out - not used)
```

---

## 📝 Notes for Developers

### To Get a Real Screenshot
1. Run the application on a system with GUI support
2. Open "本地服务" tab
3. Use the test script: `python test_coze_gui.py`
4. Or use a screen capture tool

### To Test Without GUI
```bash
# Run unit tests
python test_coze_api_settings.py

# Check syntax
python -m py_compile app/gui/local_service_tab.py
```

### To Inspect Code
```bash
# View the implementation
cat app/gui/local_service_tab.py | grep -A 50 "Coze API"

# Check test results
python test_coze_api_settings.py | grep "✅"
```
